version: '3'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./shop.db:/app/shop.db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
    environment:
      - FLASK_ENV=production 